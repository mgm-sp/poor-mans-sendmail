From: SPF-Pass Tester <INSERT-FROM-VARIABLE>
Content-Type: text/plain
Date: INSERT-DATE-VARIABLE
To: <INSERT-TO-VARIABLE>
Message-ID: <INSERT-MESSAGEID-VARIABLE>
Subject: This mail should pass SPF (+all)

This mail tests if the receiving server recognizes a passing SPF result.

This mail was sent with MAIL FROM: <INSERT-FROM-VARIABLE>

The sender domain has a permissive SPF policy:

$ dig +short txt pass.mail.mgm-sp.team
"v=spf1 a:ile-d-if.mgm-sp.team +all"

The +all mechanism means every sending IP passes SPF.
